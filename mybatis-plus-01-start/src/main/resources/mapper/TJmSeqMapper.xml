<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hzy.mapper.TJmSeqMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.hzy.entity.TJmSeq">
        <id column="id" property="id" />
        <result column="biz_model" property="bizModel" />
        <result column="value" property="value" />
        <result column="incr_interval" property="incrInterval" />
        <result column="pattern" property="pattern" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, biz_model, value, incr_interval, pattern
    </sql>

</mapper>
